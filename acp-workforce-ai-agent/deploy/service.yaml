apiVersion: v1
kind: Service
metadata:
  name: acp-workforce-ai-agent
  namespace: ai-agents
  labels:
    app: acp-workforce-ai-agent
spec:
  type: ClusterIP
  selector:
    app: acp-workforce-ai-agent
  ports:
    - port: 3001
      targetPort: 10000
      protocol: TCP
      name: http
